// Multiply: Define two numbers whose product is 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

// Random: Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Ensures number > 0

// Modulus: Define two numbers where the remainder is 4
const num3 = 16;
const num4 = 6;
const mod = num3 % num4; // 16 % 6 = 4

// Max: Find the maximum number in a set
const max = Math.max(1, 5, 10, 20);
